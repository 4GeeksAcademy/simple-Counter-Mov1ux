import React from "react";

const SecondsCounter = (props) => {
	return (
		<div className="text-center">
			<h1 className="text-center mt-5">Pestaña seconds counter{props.seconds}</h1>
			
		</div>
	);
};
export default SecondsCounter